@model fa25group23final.Models.OrdersReportViewModel
@{
    ViewData["Title"] = "All Orders Report";
}

<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-12">
            <h1 class="display-5">
                <i class="bi bi-cart"></i> All Orders Report
            </h1>
            <p class="text-muted">Order-level analysis grouped by order number</p>
            <hr />
        </div>
    </div>

    <!-- Filters -->
    <partial name="_FilterPartial" model="Model.Filters" />

    <!-- Summary Stats -->
    <div class="row mb-3">
        <div class="col-md-3">
            <div class="card bg-light">
                <div class="card-body text-center">
                    <h6 class="card-subtitle mb-2 text-muted">Total Orders</h6>
                    <h3 class="card-title mb-0">@Model.TotalRecords</h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-success text-white">
                <div class="card-body text-center">
                    <h6 class="card-subtitle mb-2">Total Revenue</h6>
                    <h3 class="card-title mb-0">@Model.Results.Sum(r => r.OrderTotal).ToString("C")</h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-info text-white">
                <div class="card-body text-center">
                    <h6 class="card-subtitle mb-2">Total Profit</h6>
                    <h3 class="card-title mb-0">@Model.Results.Sum(r => r.Profit).ToString("C")</h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-light">
                <div class="card-body text-center">
                    <h6 class="card-subtitle mb-2 text-muted">Avg Profit Margin</h6>
                    <h3 class="card-title mb-0">
                        @if (Model.Results.Any())
                        {
                            @($"{Model.Results.Average(r => r.ProfitMarginPercent):F2}%")
                        }
                        else
                        {
                            @:0.00%
                        }
                    </h3>
                </div>
            </div>
        </div>
    </div>

    <!-- Results Table -->
    <div class="card">
        <div class="card-header bg-success text-white">
            <h5 class="mb-0">Report Results</h5>
        </div>
        <div class="card-body p-0">
            @if (Model.Results.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-hover mb-0">
                        <thead class="table-dark">
                            <tr>
                                <th>Order #</th>
                                <th>Date</th>
                                <th>Customer</th>
                                <th class="text-center">Items</th>
                                <th class="text-end">Order Total</th>
                                <th class="text-end">Total Cost</th>
                                <th class="text-end">Profit Margin</th>
                                <th class="text-end">Margin %</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var row in Model.Results)
                            {
                                <tr>
                                    <td>
                                        <strong class="text-primary">#@row.OrderNumber</strong>
                                    </td>
                                    <td>@row.OrderDate.ToString("MM/dd/yyyy")</td>
                                    <td>@row.CustomerName</td>
                                    <td class="text-center">
                                        <span class="badge bg-secondary">@row.TotalQuantity</span>
                                    </td>
                                    <td class="text-end">
                                        <strong>@row.OrderTotal.ToString("C")</strong>
                                    </td>
                                    <td class="text-end text-muted">@row.TotalCost.ToString("C")</td>
                                    <td class="text-end">
                                        <strong class="@(row.Profit >= 0 ? "text-success" : "text-danger")">
                                            @row.Profit.ToString("C")
                                        </strong>
                                    </td>
                                    <td class="text-end">
                                        <span class="badge @(row.ProfitMarginPercent >= 0 ? "bg-success" : "bg-danger")">
                                            @row.ProfitMarginPercent.ToString("F2")%
                                        </span>
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot class="table-light">
                            <tr>
                                <th colspan="4" class="text-end">Totals (Current Page):</th>
                                <th class="text-end">@Model.Results.Sum(r => r.OrderTotal).ToString("C")</th>
                                <th class="text-end">@Model.Results.Sum(r => r.TotalCost).ToString("C")</th>
                                <th class="text-end">@Model.Results.Sum(r => r.Profit).ToString("C")</th>
                                <th></th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-info m-3">
                    <i class="bi bi-info-circle"></i> No orders found matching the selected criteria.
                </div>
            }
        </div>
    </div>

    <!-- Pagination -->
    @if (Model.TotalPages > 1)
    {
        <nav aria-label="Report pagination" class="mt-3">
            <ul class="pagination justify-content-center">
                <li class="page-item @(!Model.HasPreviousPage ? "disabled" : "")">
                    <a class="page-link" 
                       href="@Url.Action("Orders", new { 
                           PageNumber = Model.CurrentPage - 1,
                           Model.Filters.StartDate,
                           Model.Filters.EndDate,
                           Model.Filters.SortBy,
                           Model.Filters.CustomerId
                       })">
                        Previous
                    </a>
                </li>

                @for (int i = Math.Max(1, Model.CurrentPage - 2); i <= Math.Min(Model.TotalPages, Model.CurrentPage + 2); i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <a class="page-link" 
                           href="@Url.Action("Orders", new { 
                               PageNumber = i,
                               Model.Filters.StartDate,
                               Model.Filters.EndDate,
                               Model.Filters.SortBy,
                               Model.Filters.CustomerId
                           })">
                            @i
                        </a>
                    </li>
                }

                <li class="page-item @(!Model.HasNextPage ? "disabled" : "")">
                    <a class="page-link" 
                       href="@Url.Action("Orders", new { 
                           PageNumber = Model.CurrentPage + 1,
                           Model.Filters.StartDate,
                           Model.Filters.EndDate,
                           Model.Filters.SortBy,
                           Model.Filters.CustomerId
                       })">
                        Next
                    </a>
                </li>
            </ul>
        </nav>
    }

    <!-- Back Button -->
    <div class="row mt-4">
        <div class="col-12">
            <a asp-action="Index" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Back to Reports Dashboard
            </a>
        </div>
    </div>
</div>
